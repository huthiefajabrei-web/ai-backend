rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // All uploads go through the backend Admin SDK.
    // Public read URLs are generated server-side; direct client writes are blocked.
    match /{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if false;
    }
  }
}
